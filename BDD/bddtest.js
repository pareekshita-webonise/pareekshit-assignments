describe('looping should be defined', function () {
    it('it calculates fibonacci series', function () {
       expect(looping(10)).toEqual (55);
    });
});

describe('looping should be defined', function () {
    it('it calculates fibonacci series', function () {
       expect(looping(0)).toEqual (1);
    });
});

describe('looping should be defined', function () {
    it('it calculates fibonacci series', function () {
       expect(looping(1)).toEqual (1);
    });
});
